package com.kingc.hytale.factions.model;

/**
 * Chat modes for faction messaging.
 */
public enum ChatMode {
    /** Normal public chat. */
    PUBLIC,
    /** Messages only visible to faction members. */
    FACTION,
    /** Messages visible to faction and allies. */
    ALLY
}
